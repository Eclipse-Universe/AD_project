# Large raw data files (exceed GitHub's comfortable size limits) — keep them local only.
data/train.csv
data/test.csv

# Common Python/Jupyter noise
__pycache__/
*.pyc
.ipynb_checkpoints/
output.csv

# Experiment outputs are regenerable from code (git history) + data;
# the meaningful record lives in docs/EXPERIMENT_LOG.md, not the raw csv.
outputs/
